Here's where things stand on TST_010_0010 at the moment.

This looks like a very early setup on the shot, essentially a v001 starting point. The script is built around a single source clip read in at the working range of 1 to 182, with the standard pipeline information and burnins groups wired in, plus a guide backdrop and a sticky note flagging where parameters need to be adjusted. The bulk of the node graph is made up of basic building blocks, a handful of merges, some rectangles, a checkerboard, and an expression, which suggests the comp is still in a layout or template stage rather than active integration work. There is no sign of CG renders, mattes, or finished plate work beyond the one source read, so the creative compositing has not really begun yet.

For remaining work, almost everything is still ahead. The Write node exists but has no output path set, which means nothing can actually be rendered out, and the sticky note indicates parameters still need to be dialed in before this script does anything meaningful.

Overall completion is around 10 percent.
